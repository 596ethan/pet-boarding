spring:
  datasource:
    url: jdbc:h2:mem:pet_boarding;MODE=MySQL;DATABASE_TO_LOWER=TRUE;CASE_INSENSITIVE_IDENTIFIERS=TRUE
    username: sa
    password:
  flyway:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

pet-boarding:
  auth:
    demo-token: demo-token-admin
